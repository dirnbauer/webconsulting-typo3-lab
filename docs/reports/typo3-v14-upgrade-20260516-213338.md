# TYPO3 v14 Upgrade Report

Generated: 2026-05-16 21:33:38 Europe/Vienna

## Scope

Extension: `packages/site_package`

Goal: make the extension TYPO3 14-only, remove remaining TYPO3 13 support,
use TYPO3 APIs, update static analysis to PHPStan maximum level with the
current TYPO3 PHPStan extension, add green-path CI, and refresh documentation.

## Skill Passes

### typo3-extension-upgrade

Before: root project dependencies already target TYPO3 `^14.3`; the local
extension package had no explicit PHP requirement or release version and used
`dev-main` from the root project.

Suggested changes: set the extension release to `14.0.0`, require PHP `^8.2`,
keep TYPO3 core dependencies at `^14.3`, and update the root requirement to
`webconsulting/site-package:^14.0`.

After: Composer locks `webconsulting/site-package` as `14.0.0`; TYPO3 core
constraints remain `^14.3`.

### typo3-conformance

Before: `ext_emconf.php` still allowed `blog` `13.0.0-14.99.99`; PHPStan
pointed at a removed package path and did not analyze the site package.

Suggested changes: drop the v13 `blog` constraint, use `Build/phpstan` as the
canonical project-level configuration location, and analyze the site package
PHP entry points.

After: `ext_emconf.php` no longer allows the v13 blog range and PHPStan
analyzes the extension at `level: max`.

### typo3-security

Before: extension code uses PSR-15 middleware, dependency injection, and
TYPO3 PageRenderer. No custom JavaScript import-map parsing or response body
rewriting was present.

Suggested changes: keep the TYPO3 API-first implementation and verify the
middleware only reads request query parameters and loads known static module
specifiers.

After: no custom import-map rewriting was added; the extension continues to
use TYPO3's PageRenderer API.

### security-audit

Before: no dynamic include, command execution, unserialize, or user-controlled
file access patterns were found in the extension.

Suggested changes: add CI checks and keep the middleware narrowly scoped to
Visual Editor edit mode.

After: GitHub Actions runs the local quality suite. Security grep checks found
no dynamic execution, unserialize, dynamic include, or file-read sink in the
extension.

### typo3-testing

Before: no `Build/Scripts/runTests.sh` or GitHub Actions workflow was present.

Suggested changes: add a focused quality runner for Composer validation, PHP
linting, TYPO3 YAML linting, and PHPStan maximum level.

After: `Build/Scripts/runTests.sh` and `.github/workflows/quality.yml` were
added.

### typo3-docs

Before: the project had Markdown README files but no TYPO3 extension
`Documentation/` tree for the local extension.

Suggested changes: add minimal TYPO3 RST documentation with `guides.xml`,
`Index.rst`, installation, configuration, and development pages.

After: `packages/site_package/Documentation/` now documents installation,
site sets, Cowriter preload behavior, and development checks.

Docs extraction: `extract-all.sh` completed for the extension and generated
PHP, Composer, and extension metadata in the local `.claude/docs-extraction`
cache. `analyze-docs.sh` could not run on this machine because the script uses
Bash associative arrays and macOS ships Bash 3.2.

## Verification

- `composer validate --no-check-publish`: passed.
- `Build/Scripts/runTests.sh -s phpLint`: passed.
- `vendor/bin/typo3 lint:yaml config/sites packages/site_package/Configuration/Sets`:
  passed for 19 YAML files.
- `vendor/bin/phpstan analyse --configuration=Build/phpstan/phpstan.neon --no-progress`:
  passed at `level: max`.
- `Build/Scripts/runTests.sh -s ci`: passed.
- TYPO3 docs validation: passed; `rst2html.py` is not installed, so the
  optional docutils syntax check was skipped by the validator.
- TYPO3 docs rendering: passed and generated local HTML output under the
  ignored `Documentation-GENERATED-temp/` directory.

## Source References

- TYPO3 14.3 extension testing documentation recommends automated linting,
  static analysis with PHPStan, and organized test commands.
- TYPO3 project testing documentation uses `Build/phpstan/phpstan.neon` as the
  PHPStan configuration path and excludes `ext_emconf.php`.
- `saschaegerer/phpstan-typo3` 3.0.1 is the latest Composer-reported release
  and supports TYPO3 `^14.0` with PHPStan `^2.1.33`.
