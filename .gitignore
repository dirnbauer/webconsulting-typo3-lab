.DS_Store
._*
.ddev/config.local.yaml
.idea
nbproject
/var/*
!/var/labels
/vendor
/public/*
# normally we would ignore the whole fileadmin folder. but for the demo we need to keep fileadmin
!/public/fileadmin
/public/fileadmin/*
!/public/fileadmin/user_upload
/public/fileadmin/user_upload/_temp_
!/public/.htaccess
node_modules/*
apps/*/.next/
.cursor
.firecrawl/
scripts/*
/var/labels/*
/packages/bad_shop_watch/
# Separate repo (github.com/dirnbauer/typo3-agent-nexus), referenced via composer path repo
/packages/agent_nexus/
# Separate repo (github.com/dirnbauer/typo3-visual-editor-enhancements), referenced via composer path repo
/packages/visual_editor_enhancements/
# Separate repo (github.com/dirnbauer/skillflow), referenced via composer path repo
/packages/skillflow/
# Separate repo (abilities registry, strategy item 19), referenced via composer path repo
/packages/abilities/
# Separate repo (llms.txt/agents.md generator, strategy item 20), referenced via composer path repo
/packages/llms_txt/
/.ddev/traefik/
/backup-*/
/.tarballs/*
!/.tarballs/README.md
/.abilities-demo-token
/backup-abilities-reseed/

# Ad-hoc local DB dumps (20 MB+, stale the moment they are written) stay out
# of version control. Real backups live in .ddev/db_snapshots and ~/backups.
dump.sql.gz
*.sql.gz
