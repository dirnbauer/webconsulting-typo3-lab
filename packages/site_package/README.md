# Site Package

TYPO3 14-only site package/provider extension for the Visual Editor demo setup.

This extension owns the shared project configuration that used to be split
across `adminpanel_defaults` and `visual_editor_defaults`.

## Composer Package

| Field | Value |
|---|---|
| Composer name | `dirnbauer/site-package` |
| TYPO3 extension key | `site_package` |
| Extension version | `14.0.0` |
| TYPO3 support | `14.3.0-14.99.99` |
| PHP support | `^8.2` |
| Package type | `typo3-cms-extension` |
| Namespace | `Dirnbauer\SitePackage` |

TYPO3 13 support has been removed from Composer and `ext_emconf.php`.

## What This Package Provides

- Shared TYPO3 Site Sets for the demo sites:
  - `dirnbauer/site-package`
  - `dirnbauer/site-package-search`
  - `dirnbauer/site-package-blog`
  - `dirnbauer/site-package-blog-bootstrap`
  - `dirnbauer/site-package-camino`
  - `dirnbauer/site-package-desiderio-corporate`
- Frontend Admin Panel defaults:
  - Enables `config.admPanel`.
  - Enables Admin Panel access for backend users via `Configuration/user.tsconfig`.
- Shared editor defaults:
  - Sets `RTE.default.preset = cowriter`.
  - Registers the Cowriter preload middleware for Visual Editor edit mode.
- Shared template paths:
  - Adds Desiderio partial root paths for EXT:news and EXT:blog.
  - Provides Desiderio template path overrides for Blog and Desiderio Corporate sites.
- Shared Solr defaults:
  - Depends on the existing `dirnbauer/solr-defaults` Site Set.
  - Adds the numbered pagination partial path.
  - Removes the stale EXT:solr 14 `solr-jquery` include.
- MCP table configuration:
  - Exposes `tt_address` as `Addresses` for MCP dynamic tools.

## Site Sets

| Site Set | Dependencies | Purpose |
|---|---|---|
| `dirnbauer/site-package` | none | Base project defaults, RTE preset, Admin Panel, MCP table config, Cowriter preload middleware. |
| `dirnbauer/site-package-search` | `dirnbauer/site-package`, `dirnbauer/solr-defaults` | Shared Solr integration and numbered pagination partial path. |
| `dirnbauer/site-package-blog` | `dirnbauer/site-package-search`, `blog/standalone` | Blog standalone rendering with Desiderio templates. |
| `dirnbauer/site-package-blog-bootstrap` | `dirnbauer/site-package-search`, `blog/bootstrap-53` | Blog Bootstrap 5.3 demo rendering. |
| `dirnbauer/site-package-camino` | `dirnbauer/site-package-search`, `typo3/theme-camino` | Camino demo rendering. |
| `dirnbauer/site-package-desiderio-corporate` | `dirnbauer/site-package-search`, `webconsulting/desiderio-preset-corporate`, `webconsulting/desiderio-shadcnui-templates` | Desiderio corporate demo rendering. |

## File Map

| Path | Purpose |
|---|---|
| `Configuration/Sets/SitePackage/` | Base Site Set, Admin Panel TypoScript, and Cowriter RTE Page TSconfig. |
| `Configuration/Sets/Search/` | Search/Solr Site Set, numbered pagination settings, stale EXT:solr 14 jQuery cleanup. |
| `Configuration/Sets/Blog/` | Blog standalone Site Set and Desiderio template path override. |
| `Configuration/Sets/BlogBootstrap/` | Blog Bootstrap Site Set wrapper. |
| `Configuration/Sets/Camino/` | Camino Site Set wrapper. |
| `Configuration/Sets/DesiderioCorporate/` | Desiderio corporate Site Set and template path override. |
| `Configuration/RequestMiddlewares.php` | Registers the Cowriter preload middleware in the frontend stack. |
| `Configuration/Services.yaml` | Autowires `Dirnbauer\SitePackage` classes. |
| `Configuration/user.tsconfig` | Enables Admin Panel access for backend users. |
| `Classes/Middleware/CowriterPreloadMiddleware.php` | Loads t3-cowriter JavaScript modules for Visual Editor edit mode. |
| `ext_localconf.php` | Registers MCP table metadata for `tt_address`. |

## Sites Using These Sets

| Site | Dependency |
|---|---|
| `config/sites/main` | `dirnbauer/site-package` |
| `config/sites/camino` | `dirnbauer/site-package-camino` |
| `config/sites/blog` | `dirnbauer/site-package-blog` |
| `config/sites/autogenerated-390-a01a0380ca3c61428c26a231f0e49a09` | `dirnbauer/site-package-blog` |
| `config/sites/14lts` | `dirnbauer/site-package-blog-bootstrap` |
| `config/sites/mattersburg-sights` | `dirnbauer/site-package-desiderio-corporate` |
| `config/sites/typo3-vienna-camp-2026` | `dirnbauer/site-package-desiderio-corporate` plus `desiderio/content-blocks-bundle` |

## Removed Packages

The following local packages were consolidated into this package and removed:

- `packages/adminpanel_defaults`
- `packages/visual_editor_defaults`

Composer now only needs `dirnbauer/site-package` for these shared project
defaults.

## Verification

After changing this package, run the project-level checks from the repository
root:

```bash
composer validate --no-check-publish
Build/Scripts/runTests.sh -s phpstan
Build/Scripts/runTests.sh -s ci
ddev exec vendor/bin/typo3 lint:yaml config/sites packages/site_package/Configuration/Sets
ddev exec vendor/bin/typo3 extension:setup
ddev exec vendor/bin/typo3 cache:flush
```

PHPStan runs at `level: max` with the TYPO3-specific
`saschaegerer/phpstan-typo3` extension.
